<?xml version="1.0" encoding="utf-8"?>

<views:ContentPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:views="clr-namespace:Time2Plan.App.Views"
                       xmlns:viewModels="clr-namespace:Time2Plan.App.ViewModels"
                       xmlns:t="clr-namespace:Time2Plan.App.Resources.Texts"
                       xmlns:models="clr-namespace:Time2Plan.BL.Models;assembly=Time2Plan.BL"
                       x:Class="Time2Plan.App.Views.Project.ProjectEditView"
                       Title="{x:Static t:ProjectEditViewTexts.Page_Title}"
                       x:DataType="viewModels:ProjectEditViewModel"
                       Style="{StaticResource ContentPageStyle}">
    <!--<Grid RowDefinitions="Auto, *, Auto"
          RowSpacing="40">
        <Grid Grid.Row="0"
              ColumnDefinitions="*, 320"
              ColumnSpacing="56">
            <Grid Grid.Column="0"
                  RowDefinitions="Auto, Auto, Auto"
                  RowSpacing="20">
                <VerticalStackLayout Grid.Row="0"
                                     Spacing="10">
                    <Label Text="{x:Static t:ProjectEditViewTexts.Name_Label_Text}" />

                    <Entry HorizontalOptions="Fill"
                           Text="{Binding Project.Name}" />
                </VerticalStackLayout>

                <Grid Grid.Row="1"
                      RowDefinitions="Auto, Auto"
                      ColumnDefinitions="Auto, *"
                      RowSpacing="10" ColumnSpacing="17">
                    <Label Grid.Row="0" Grid.Column="0"
                           Text="{x:Static t:ProjectEditViewTexts.FoodType_Label_Text}" />
                    <Picker Grid.Row="1"
                            Grid.Column="0"
                            HorizontalOptions="Fill"
                            ItemsSource="{Binding FoodTypes}"
                            ItemDisplayBinding="{Binding ., Converter={StaticResource FoodTypeToStringConverter}}"
                            SelectedItem="{Binding Project.FoodType}" />

                    <Label Grid.Row="0" Grid.Column="1"
                           Text="{x:Static t:ProjectEditViewTexts.Duration_Label_Text}" />
                    <TimePicker Grid.Row="1" Grid.Column="1"
                                Time="{Binding Project.Duration}"
                                Format="HH:mm" />
                </Grid>

                <StackLayout Grid.Row="2"
                             Orientation="Vertical"
                             Spacing="10">
                    <Label Text="{x:Static t:ProjectEditViewTexts.ImageUrl_Label_Text}" />

                    <Entry Text="{Binding Project.ImageUrl}"/>
                </StackLayout>
            </Grid>

            <Frame Grid.Column="1"
                   HorizontalOptions="Fill" VerticalOptions="Fill"
                   Style="{StaticResource ImageFrameStyle}">
                <Image Source="{Binding Project.ImageUrl, TargetNullValue='Project_placeholder.jpg'}"
                       WidthRequest="320" HeightRequest="240"
                       Aspect="AspectFill" />
            </Frame>
        </Grid>

        <Grid Grid.Row="1"
              ColumnDefinitions="*, *"
              ColumnSpacing="40">

            <Grid Grid.Column="0"
                  RowDefinitions="Auto, *"
                  RowSpacing="30">
                <Label Grid.Row="0"
                       Text="{x:Static t:ProjectEditViewTexts.Description_Label_Text}" />

                <Editor Grid.Row="1"
                        Text="{Binding Project.Description}"
                        AutoSize="TextChanges"
                        VerticalOptions="Fill" />
            </Grid>

            <VerticalStackLayout Grid.Row="0" Grid.Column="1"
                                 Spacing="30">
                <Button Text="{x:Static t:ProjectEditViewTexts.EditActivitys_Button_Text}"
                        Command="{Binding GoToProjectActivityEditCommand}"
                        IsVisible="{Binding Project, Converter={StaticResource ModelIsNewToIsVisibleInvertedConverter}}"
                        Style="{StaticResource PrimaryButtonStyle}" />
                <Label Text="{x:Static t:ProjectEditViewTexts.ErrorUnsavedProject_Label_Text}"
                       HorizontalOptions="End"
                       IsVisible="{Binding Project, Converter={StaticResource ModelIsNewToIsVisibleConverter}}" />

                <CollectionView ItemsSource="{Binding Project.Activitys}"
                                HorizontalOptions="End">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:ActivityAmountDetailModel">
                            <HorizontalStackLayout>
                                <Frame Margin="5, 5, 20, 5"
                                       Style="{StaticResource ImageFrameStyle}">
                                    <Image Source="{Binding ActivityImageUrl, TargetNullValue='Activity_placeholder.jpg'}"
                                           WidthRequest="60" HeightRequest="60"
                                           Aspect="AspectFill" />
                                </Frame>

                                <Label VerticalOptions="Center">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="{Binding Amount}" />
                                            <Span Text=" " />
                                            <Span Text="{Binding Unit, Converter={StaticResource UnitToStringConverter}}" />
                                            <Span Text=" " />
                                            <Span Text="{Binding ActivityName}" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </HorizontalStackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </Grid>

        <Button Grid.Row="2"
                HorizontalOptions="End"
                Text="{x:Static t:ProjectEditViewTexts.Save_Button_Text}"
                Command="{Binding SaveCommand}"
                Style="{StaticResource PrimaryButtonStyle}" />
    </Grid>-->
</views:ContentPageBase>
