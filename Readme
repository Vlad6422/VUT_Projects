Code was a little bit generated by ChatGpt
Please make pull request if you want to improve something or add Comments.


Sure, here's your text in Markdown format:


To clone the repository, run the following command in your terminal:

```bash
git clone https://github.com/Vlad6422/IPP-patters.git
```

Navigate into the cloned directory:

```bash
cd IPP-patterns
```

Inside this directory, you'll find a `.sln` file. You can open it with Visual Studio. Here are the steps:

1. Open Visual Studio.
2. Navigate to `File` -> `Open` -> `Project/Solution`.
3. Select the `NavrhVzory.sln` file.

Alternatively, if you just want to view the code, you can explore each folder and check the `Program.cs` file.